---
name: remove_acknowledgement
runner_type: "python-script"
description: Remove the acknowledgement for services or hosts.
enabled: true
entry_point: remove_acknowledgement.py
parameters:
  object_type:
    description: Host or Service
    type: string
    required: true
  filters:
    description: filters matched object(s)
    type: string
    required: true
  filter_vars:
    description: variables used in the filters expression
    type: object

