---
name: remove_downtime
runner_type: "python-script"
description: Remove the downtime using its name or filters.
enabled: true
entry_point: remove_downtime.py
parameters:
  object_type:
    description: Host, Service or Downtime
    type: string
    required: true
  name:
    description: name of the downtime
    type: string
  filters:
    description: filters matched object(s)
    type: string
  filter_vars:
    description: variables used in the filters expression
    type: object
